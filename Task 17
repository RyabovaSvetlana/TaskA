// Задать одномерный массив, заполненный случайными числами. Найти сумму элементов, стоящих на нечётных позициях.
int [] numbers = (3, 7, 23, 12);
int sum = 0;

for (int i = 1; i < numbers.Lenght; i += 2);
{
    if (i % 2 ! = 0)
    {
        sum += numbers [i];
    }

}
Console.WriteLine (sum);
